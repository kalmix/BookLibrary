<Page
    x:Class="BookLibrary.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookLibrary.Views"
    xmlns:entities="using:BookLibrary.Entities.Models"
    xmlns:converters="using:BookLibrary.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:AbsolutePathConverter x:Key="PathConverter" />
    </Page.Resources>

    <Grid Padding="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Libreria" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,24"/>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Books, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="entities:Book">
                    <Grid Padding="16" Margin="0,0,0,8" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Thumbnail -->
                        <Border Grid.Column="0" Width="48" Height="64" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Margin="0,0,16,0">
                            <Image Source="{x:Bind CoverImagePath, Converter={StaticResource PathConverter}}" Stretch="UniformToFill"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind Author}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        
                        <!-- Buttons -->
                        <Button Grid.Column="2" Margin="12,0" VerticalAlignment="Center"
                                Click="OpenPdf_Click" Tag="{x:Bind}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8A7;" FontSize="16"/>
                                <TextBlock Text="Abrir PDF"/>
                            </StackPanel>
                        </Button>
                                        
                        <Button Grid.Column="3" Foreground="{ThemeResource SystemFillColorCriticalBrush}" VerticalAlignment="Center"
                                Click="Delete_Click" Tag="{x:Bind}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                                <TextBlock Text="Eliminar"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
