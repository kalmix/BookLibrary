<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BookLibrary.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Digital Library">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <Grid x:Name="AppTitleBar" Height="40" Background="Transparent">
            <!-- Icon and Title -->
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center" Spacing="12" HorizontalAlignment="Left">
                <Image Source="ms-appx:///Assets/Square44x44Logo.scale-200.png" Width="16" Height="16" />
                <TextBlock Text="Libreria" FontSize="12" FontFamily="Segoe UI" FontWeight="Normal" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
            </StackPanel>

            <!-- Search bar bruv -->
            <AutoSuggestBox x:Name="TitleBarSearchBox" 
                            QueryIcon="Find" 
                            PlaceholderText="Buscar libros por título o autor..." 
                            Width="400" 
                            Height="32"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            TextChanged="TitleBarSearchBox_TextChanged"/>
        </Grid>

        <NavigationView Grid.Row="1" x:Name="NavView" PaneDisplayMode="Left" SelectionChanged="NavView_SelectionChanged" ItemInvoked="NavView_ItemInvoked" IsSettingsVisible="False" Background="Transparent">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="Home" Icon="Library" Content="Libreria"/>
                <NavigationViewItem Tag="AddBook" Icon="Add" Content="Agregar Nuevo Libro"/>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="ThemeNavItem" Content="Modo Claro" SelectsOnInvoked="False">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE706;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            
            <Frame x:Name="ContentFrame" CornerRadius="8,0,0,0" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,1,0,0">
            </Frame>
        </NavigationView>
    </Grid>
</Window>
